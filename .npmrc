; Some three.js / @react-three peers resolve strictly under npm 7+.
; Install with legacy peer resolution instead of a "postinstall" that
; re-ran "npm install" and recursed until it failed.
legacy-peer-deps=true
